# Publish the core JavaScript package as @unpack-js/core

The first public JavaScript package will be named `@unpack-js/core` rather than `unpack`. This gives the project a scoped package namespace for the core bundler API while leaving room for future companion packages without overloading the unscoped `unpack` package name.
