# JavaScript lifecycle webpack alignment

This matrix compares webpack behavior with current Unpack behavior for exposed JavaScript lifecycle scenarios and is updated as each lifecycle fix lands. Each row should be filled from direct webpack comparison tests or code-backed Unpack observations, then classified before implementation work starts.

Callback timing comparisons should first distinguish synchronous throws, synchronous return, and asynchronous callback invocation. They should not lock tests to microtask, `process.nextTick`, or macrotask ordering unless a specific webpack behavior has user-visible semantics that require that precision.

Webpack behavior should come from a repo-managed, pinned webpack dependency used by comparison tests. Local webpack source checkouts may be used to understand implementation details, but the matrix should not depend on a personal checkout path as the behavior oracle.

Matrix rows should be backed by committed webpack comparison tests. One-off exploration scripts may help design those tests, but they should not be the source of record for matrix conclusions.

Lifecycle comparison tests should live in `packages/unpack/test/webpack-lifecycle-alignment.test.ts` so webpack-alignment scenarios stay separate from ordinary API regression tests in `packages/unpack/test/api.test.ts`.

When a scenario reveals a current difference, the first committed test may be an observation-style passing test that asserts webpack's behavior and current Unpack behavior separately while the matrix classifies the difference as an alignment gap. The fix should then tighten that scenario into a shared alignment assertion once Unpack behavior has changed.

| API scenario | Webpack behavior | Current Unpack behavior | Classification | Required test | Required fix |
| --- | --- | --- | --- | --- | --- |
| `unpack(options, callback?)` validation and initialization error timing | Invalid `mode` without a callback throws synchronously with `ValidationError`; invalid `mode` with a callback returns no compiler and invokes the callback asynchronously with `ValidationError` and no `Stats`. | Invalid options without a callback throw synchronously with `TypeError`. With a valid callback, option validation and Compiler initialization failures return `null` and invoke the callback asynchronously with the error and no `Stats`. | Callback timing, return value, and Stats availability are aligned. Retaining wrapper `TypeError` taxonomy instead of webpack schema `ValidationError` is a Documented Webpack Deviation in ADR 0135. | `packages/unpack/test/webpack-lifecycle-alignment.test.ts` verifies the shared validation timing and return contract, records the error-taxonomy boundary, and exercises a public native-initialization failure. | None. |
| `unpack(options, callback?)` callback timing | Returns a compiler synchronously; callback is asynchronous with `err === null` and `Stats`; returned compiler remains runnable after the callback. | Returns a compiler synchronously; callback is asynchronous with `err === null` and `Stats`; returned compiler remains runnable until the caller explicitly closes it. | Aligned. | `packages/unpack/test/webpack-lifecycle-alignment.test.ts` verifies callback timing and a successful rerun through shared assertions. | None. |
| `compiler.run(callback)` callback timing | A non-conflicting `run` returns synchronously and invokes its callback asynchronously. | A non-conflicting `run` returns synchronously and invokes its callback asynchronously. | Aligned. | `packages/unpack/test/webpack-lifecycle-alignment.test.ts` verifies successful, Compilation Error, and infrastructure-failure delivery. | None. |
| `compiler.run(callback)` `err` versus `Stats` | Parse errors complete with `err === null`, a `Stats` object, and `stats.hasErrors() === true`. | Parse errors complete with `err === null`, a `Stats` object, and `stats.hasErrors() === true`. | Aligned. | `packages/unpack/test/webpack-lifecycle-alignment.test.ts` observes parse-error stats semantics. | None. |
| `compiler.run(callback)` infrastructure failure | Output-write and other infrastructure failures invoke the callback with an error and no `Stats`, leaving the Compiler able to reach a later lifecycle operation. | Matches webpack's error-versus-Stats boundary; Unpack uses its named Infrastructure Error taxonomy. | Aligned for delivery and state, without claiming exact error classes or messages. | `packages/unpack/test/webpack-lifecycle-alignment.test.ts` verifies asynchronous exactly-once delivery and a subsequent close. | None. |
| Concurrent `compiler.run(callback)` | The conflicting callback is synchronous with `ConcurrentCompilationError` and no `Stats`; the active run remains intact. | The conflicting callback is asynchronous with `ConcurrentRunError` and no `Stats`; the active run remains intact and a later run succeeds. | Documented Webpack Deviation in ADR 0136: Unpack keeps every accepted run callback asynchronous. | `packages/unpack/test/webpack-lifecycle-alignment.test.ts` records both timings, error boundaries, callback counts, and active-run outcome. | None. |
| `compiler.run(callback)` terminal delivery and reentry | The Compiler clears its running state before callback delivery, so a successful callback can synchronously start another run. | Matches webpack; run state remains active through terminal preparation and is cleared immediately before callback delivery. | Aligned. | `packages/unpack/test/webpack-lifecycle-alignment.test.ts` starts the second run inside the first callback and verifies both callbacks exactly once. | None. |
| `compiler.close(callback)` lifecycle behavior | Close may invoke its callback synchronously, does not reject an active run, is not terminal, and permits repeated close and later run calls. | Close is always asynchronous and terminal. Active run/watch work rejects close; idle close enters `closing` synchronously, rejects new run/watch work, coalesces callers, waits cache settlement and shutdown, releases native resources, and enters `closed`. Repeated close is asynchronous idempotent success; later run fails asynchronously. | Documented Webpack Deviation in ADR 0136 for deterministic callback timing and Rust-native resource ownership. | Lifecycle comparisons cover idle, repeated, active-run, post-close, callback count, and timing behavior; public API regressions cover concurrent close and the closing race. | None. |
| `compiler.watch(watchOptions, handler)` initial callback behavior | TBD | TBD | TBD | TBD | TBD |
| `compiler.watch(watchOptions, handler)` while a manual run is active | The conflicting handler is synchronous with `ConcurrentCompilationError` and no `Stats`; the active run remains intact. | The conflicting handler is asynchronous with `ConcurrentRunError` and no `Stats`; the active run remains intact and a later run succeeds. | Documented Webpack Deviation in ADR 0136: Unpack keeps conflict delivery asynchronous. This row covers only Compiler operation arbitration, not Watch Session behavior. | `packages/unpack/test/webpack-lifecycle-alignment.test.ts` records timing, error boundaries, callback counts, and active-run recovery for both implementations. | None. |
| `Watching.invalidate()` behavior | TBD | TBD | TBD | TBD | TBD |
| `Watching.close(callback)` behavior | TBD | TBD | TBD | TBD | TBD |
| `Stats.hasErrors()` behavior | Returns `false` for a successful Compilation and `true` for a completed Compilation containing a parse error; both runs complete with `err === null` and `Stats`. | Matches webpack for successful and parse-error Compilations. | Aligned for the supported Compilation Error boundary. | `packages/unpack/test/webpack-lifecycle-alignment.test.ts` verifies both outcomes with shared baseline Stats assertions. | None. |
| `Stats.toJson()` lifecycle shape | The selected baseline projection reports Assets, errors, warnings, and Output Path for successful and error-bearing Compilations; each call produces a fresh report object. webpack's default report contains additional fields outside Unpack's current surface. | Reports the supported Assets, errors, warnings, Output Path, and Watch Dependency Set projection, and returns an isolated snapshot on each call. | Aligned for the supported baseline projection. The deliberately smaller report surface remains documented by ADR 0041. | The lifecycle comparison verifies the shared baseline projection; `packages/unpack/test/api.test.ts` verifies snapshot isolation. | None. |
